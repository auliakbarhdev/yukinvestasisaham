---
// Import SecondaryCTA component for use in this module
import SecondaryCTA from "@components/ui/buttons/SecondaryCTA.astro";
import Icon from "@components/ui/icons/Icon.astro";

// Define props from Astro
const { accounts } = Astro.props;
---

<section
  class="mx-auto max-w-[85rem] px-4 py-10 sm:px-6 lg:px-8 lg:py-14 2xl:max-w-full"
  id="account-type"
>
  <!-- Section heading and sub-heading -->
  <div class="mx-auto mb-10 max-w-2xl text-center lg:mb-14">
    <h2
      class="text-balance text-2xl font-bold tracking-tight text-neutral-800 dark:text-neutral-200 md:text-4xl md:leading-tight"
    >
      {accounts.title}
    </h2>
    <p class="mt-1 text-pretty text-neutral-600 dark:text-neutral-400">
      {accounts.subTitle}
    </p>
  </div>
  <!-- Contains two main product blocks -->
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 items-center justify-center gap-4">
    <!-- product details -->
    {
      accounts.items.map(item => (
        <div
          class="w-full h-full rounded-xl bg-gray-600 p-6"
        >
          <div class="mb-4">
            <h3 class="text-2xl font-bold text-neutral-100 sm:text-3xl">
              {item.name}
            </h3>
          </div>
          <!-- Features list - automatically created by mapping over `features` array -->
          <ul class="mb-6 space-y-2 text-neutral-300">
            {
              item.list.map((feature: string) => (
                <li class="flex items-center gap-1.5">
                  <Icon name="checkCircle" />

                  <span>{feature}</span>
                </li>
              ))
            }
          </ul>
        </div>
      ))
    }
  </div>
</section>
